import React from 'react'

export default class extends React.Component {
        componentDidMount() {
                window.location.replace("/")
        }

        render() {
                return null
        }
}
